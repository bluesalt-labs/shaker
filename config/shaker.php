<?php defined('SYSPATH') or die('No direct access allowed.');

return array(
    // This file may not be needed, and may instead be replaced by the general application config file provided by Lumen.
    // However, this it would probably be easier to understand if this config file exists.
    // Things like the application name, various global settings, etc. could be configured here.
);
